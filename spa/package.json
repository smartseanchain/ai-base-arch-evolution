{
  "name": "ai-base-arch-spa",
  "private": true,
  "version": "0.0.0",
  "description": "全站 SPA（Vite）；npm run sync = 仓库根 sync_spa_public。merge/spa 收束见 ../docs/MERGE_AND_RELEASE_CHECKLIST.md#pre-merge · #pre-merge-partials-sequence · ../maintainer-hub.html#mh-spine-map · #mh-boundaries · #mh-reader-admin-matrix · 根目录 make help",
  "type": "module",
  "scripts": {
    "gen-nav": "cd .. && python3 scripts/gen_nav_links_ts.py --write",
    "sync": "cd .. && python3 scripts/sync_spa_public.py",
    "predev": "npm run gen-nav && npm run sync",
    "prebuild": "npm run gen-nav && npm run sync",
    "dev": "vite",
    "build": "vite build && node scripts/postbuild-copy-404.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "~5.7.3",
    "vite": "^6.4.2"
  }
}

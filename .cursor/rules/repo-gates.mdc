---
description: 合并前 validate、双轨 MPA+SPA、manifest 人审与分析不写 HTML（对齐 AGENTS.md）
alwaysApply: true
---

# 仓库闸门与边界（速查）

完整说明见仓库根目录 **`AGENTS.md`**、**`CONTRIBUTING.md`**；**维护者文档主线表** **`docs/README.md`** · [文档主线](docs/README.md#docs-spine)；命令表 **`scripts/README.md`**；架构 **`docs/ARCHITECTURE.md`**；**技术 / 内容 / 推演** 对照 **`docs/ARCHITECTURE_ONE_PAGER.md`** · [三架构对照](docs/ARCHITECTURE_ONE_PAGER.md#three-architectures)；CI 双轨见 **`docs/README.md`** 文首。**读者预期**（枢纽记忆、站内 **`docs/*.md`** 在静态部署下的行为、发布前人工清单）：**`docs/PLATFORM_CAPABILITY_MAP.md`** §7 · **`docs/SITE_REVIEW_THREE_PASSES.md`** · [四角色复查](docs/SITE_REVIEW_THREE_PASSES.md#four-perspectives-review)（与 **`AGENTS.md`** 读者条一致）。**扩展性与平台进化**（插槽、四轨、阶段跑道、[智能化与自动化边界](docs/PLATFORM_EXTENSIBILITY_AND_EVOLUTION.md#automation-and-evolution)、产品/工程习惯、新增能力检查单）：**`docs/PLATFORM_EXTENSIBILITY_AND_EVOLUTION.md`** · **`docs/MERGE_AND_RELEASE_CHECKLIST.md`** · **`docs/INTEGRATION_AND_READONLY_API.md`** · **`docs/schemas/README.md`**。

## 必须遵守

- 建议合并或宣称「可以提交」前：在仓库根执行 **`make validate`**（别名 **`make phase-1`**；与 **`.githooks/pre-commit`**、**`ci.yml` · validate** 一致）。与 CI 子测对齐时一次执行 **`make merge-ready`**（**`validate`** + **`test-readonly-api`**（**`test_readonly*.py`**）+ **`test-admin-console`**），见 **`docs/MERGE_AND_RELEASE_CHECKLIST.md`**。
- 迭代中可跑 **`make test`**（registry Schema + 单测 + navLinks + 沉淀/趋势 Schema）；**不替代**合并前的 **`make validate`**（见 **`AGENTS.md`**）。
- **`make analyze`**（**`evolution_pkg.pipeline.runner`**）在写入快照/沉淀**之前**复用与 **`run_validate.sh`** 相同的闸门至**单测**（含 **registry Schema**、对账、**navLinks**、顶栏）；写入后再校沉淀/趋势与快照 Schema，并以 **`analysis_engine.py --check`** 收尾。
- **勿**设计或实现绕过 **`review_state` / 人审** 对 **`evolution-manifest.json`** 的自动写入。
- **`analysis_engine`** 只产出结构化 JSON（如 **`analysis-snapshot.json`**）；**不写** 根目录 **`.html`** 叙事正文。

## 双轨呈现

- **`make validate`** 以根目录 **MPA** 为默认真源。
- 改 **MPA** 枢纽页首屏（`lead` / `read-hint`、路径条短文案）时对照 **`docs/ARCHITECTURE_ONE_PAGER.md`**「内容与呈现」与 **`docs/SITE_REVIEW_THREE_PASSES.md`** §3.5（与 **`AGENTS.md`** 一致）。改 **`index.html`** 读站指路等后，若维护 **SPA**：须 **`make spa-sync`**（或 **`npm run sync`**），见 **`spa/README.md`**。
- 维护 **SPA** 时：增删注册 **`pages`** 须同步 **`spa/src/navLinks.ts`** 与 **`scripts/evolution-registry.json`**；**`SpaLayout`** 快捷链与 **`partials/skip-bar.inc.html`** 同序（含 **`/#read-guide`**）；总览 hash 的标题/读屏见 **`spa/src/spaRouteMeta.ts`**，iframe **`title`** 见 **`spa/src/LegacyFrame.tsx`**。CI **`spa-build`** 按路径触发，**不替代** JSON 闸门。
- 编辑 **`spa/nav.config.json`** 时附加 **`spa-nav-config.mdc`**；**`spa/src/**`** → **`spa-nav-registry.mdc`**；**`scripts/evolution-registry.json`** → **`evolution-registry.mdc`**。

## 阶段化基础设施（编排 / Kafka / 生产库）

- 立项或改 CI 与上述相关时：先 **`docs/PHASED_UPGRADE_EXECUTION_GUIDE.md`** · **`docs/ARCHITECTURE_UPGRADE_ROADMAP.md`** §1；**勿**以第二套流程替代 **`run_validate.sh`** / **`make validate`** 作为契约合并真源。

## Fork

- **`.github/ISSUE_TEMPLATE/pipeline-triage.md`** 文首 CONTRIBUTING 链接若指向他仓，Fork 后请改为本仓库 URL（见 **CONTRIBUTING.md**）。**`.github/ISSUE_TEMPLATE/config.yml`** · **`contact_links`** 同理。
